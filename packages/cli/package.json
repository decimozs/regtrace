{
	"name": "@regtrace/cli",
	"version": "0.10.1",
	"description": "Regtrace CLI — evaluate LLM outputs with deterministic and LLM-judged metrics",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/decimozs/regtrace.git"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit",
		"lint": "biome check .",
		"format": "biome check --write .",
		"lint:staged": "biome check --staged --no-errors-on-unmatched"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/js-yaml": "^4.0.9"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"commander": "13",
		"date-fns": "^4.4.0",
		"dotenv": "^17.4.2",
		"js-yaml": "^4.2.0",
		"nanoid": "^5.1.11",
		"zod": "^4.4.3"
	}
}
